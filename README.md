Tracked Element On Scroll
===========
> add class on element when it show in viewport
## Example
```js
const element = document.querySelector(".yo");
trackedElementOnScroll(element, "fadeIn", 25, "fadeOut");
```